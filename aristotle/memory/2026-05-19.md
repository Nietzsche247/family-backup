# 2026-05-19 — L45 Root Cause Found + SUB.7 Patched

## Summary
Overnight investigation revealed the true root cause of the 62-hour outage: `heartbeat-switcher.ps1` kills the gateway and launches a competing one twice daily at 08:00 and 20:00 MST. Fix applied. SUB.2-5 documentation deferred to later today. Rail Kit work next.

## Key Events
- **Overnight** — PID 337452 survived 19+ hours. F1a autonomously caught a 6-restart cascade at 20:00 MST (crash-log shows 6 competing gateway pairs). F4 caught all 12 orphan exits. Watchdog pulsed healthy through the entire event.
- **07:59** — Aaron forwarded Plato's overnight analysis. Plato correctly identified the crash-log burst but attributed it to watchdog escalation (wrong vector).
- **08:00** — Another burst hit (daytime heartbeat switch). Two more F4 bootstrap loads + exits in crash-log.
- **~08:05** — Ran investigation: watchdog log (clean hourly pulses), task-gateway.log (F2 skipping cleanly all night), crash-log timeline, system events. Found `L45_CRASH_LOOP_CEILING_HIT: 6 restarts in 600s` at 20:03:14 MST in task-gateway.log — F1a working.
- **~08:10** — First analysis attributed restart to config-reload code path. Aaron pushed back: clawdbot log shows FULL gateway init (plugin chain, viewer port collision), not a hot-reload. The config reload at 03:00:02 UTC ran inside PID 337452; something ELSE spawned competing gateways 12 seconds later.
- **~08:15** — Aaron directed investigation of heartbeat task Actions. Read `heartbeat-switcher.ps1` — **found the smoking gun**: lines 43-54 kill the gateway via `Stop-Process -Force` then unconditionally launch `gateway-resilient.cmd`, all wrapped in silent error handling.
- **~08:38** — SUB.7 + SUB.7a written. `heartbeat-switcher.ps1` patched (kill+restart block removed). Committed SHA `45a5038`.
- **08:40** — Aaron directed memory/state update (this file).

## Root Cause Chain (Complete)
1. `heartbeat-switcher.ps1` fires at 20:00 MST Fri 5/15
2. Writes new `heartbeat.every` to `clawdbot.json` (hot-reload works fine)
3. Tries `Stop-Process -Force` on port 18792 owner — likely succeeds (kills live gateway)
4. Launches `gateway-resilient.cmd` — new gateway starts from scratch
5. New gateway's first heartbeat tick hits L44 `skill_manage` / `registerInMemosStore` bug
6. Gateway crashes → supervisor retries → crashes again → periodic task retries → watchdog escalates
7. Five respawn vectors stacked: supervisor loop, periodic trigger, RestartOnFailure (999), watchdog, heartbeat-switcher
8. 62-hour wedge cycle until manual intervention

## L45 Sub-Lessons (Complete Inventory)
- **SUB.1** — F1b TryParse PowerShell 7+ overload bug. Execution validation required.
- **SUB.6** — RestartOnFailure Count=999 is a hidden respawn vector. Removed.
- **SUB.7** — heartbeat-switcher.ps1 kill+restart is the 5th respawn vector AND the root trigger. Removed.
- **SUB.7a** — Mechanism claims require evidence (file, line, log entry), not plausibility.
- **SUB.2-5** — Deferred to later today (gateway.cmd ACLs, F2 keystone, orphan cleanup, agents-can't-self-kill)

## Overnight Validation Results
- F1a: ✅ Caught 6-restart cascade autonomously at 20:00 MST
- F1b: ✅ Healthy pulses all night (22:00→07:00, 10 consecutive)
- F2: ✅ Every 5-min task firing exited cleanly ("Skipping wrapper")
- F4: ✅ Caught 12 orphan process exits across 3 bursts
- F3: Still un-exercised (bug hasn't reproduced — expected with live gateway stable)

## Next Steps (from Aaron)
1. ~~Confirm overnight state~~ ✅ PID 337452, 19h+ uptime
2. ~~SUB.7 investigation + patch~~ ✅ Root cause found, fix applied
3. Document SUB.2-5 (morning, not urgent)
4. Run `source-truth-preflight` against OmniPoolsAZ repo — first real Rail Kit invocation
5. npm install in Omni repo → rerun depcruise → real architecture map
6. Install ast-grep → first pattern scan
7. 20:00 MST tonight: passive SUB.7 verification (heartbeat switch should be no-op)
