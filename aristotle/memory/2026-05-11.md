# 2026-05-11

## Phase 3: NorthStar Bridge — Emitter Build

### What was built
- **ledger-emitter.js** (ESM module) at `dist/ledger-emitter.js` in memos-local extension
  - Fires on agent_end after MemOS chunk capture
  - Conservative: defaults to memory_capture / routine_session_capture
  - Retry queue: failed Ledger writes → `~/.openclaw/memos-local/ledger-retry-queue.jsonl`
  - Never blocks agent_end — Ledger is optional, memory is not
  - Loads goal event IDs from `C:/North_Star_Projects/ledger/goal-event-ids.json`

### Injection point
- `dist/index.js` line ~2010, after `worker.enqueue(captured)` and `telemetry.trackMemoryIngested()`
- Uses dynamic `import("./ledger-emitter.js")` (ESM) with `.catch()` for graceful failure
- Three iterations on import syntax: CommonJS require() failed (empty exports in ESM context), converted to ESM imports/exports + dynamic import()

### Schema work completed
- v1.2 columns added to Ledger: event_subtype, memory_chunk_id (db.js ALTER TABLE)
- 4 new event types added to schemaV1_1.js: memory_capture, task_completed, agent_recovery, configuration_change
- 4 goal_declarations seeded from northstar_goals.json (event IDs in goal-event-ids.json)
- 5 test events emitted manually, all 201 OK
- Total Ledger events: 50

### Blockers encountered
1. Daedalus sub-agents bail in 2-3 seconds (model issue?) — had to finish schema+seeding manually
2. dist/index.js edits don't load until gateway PROCESS restarts (not just SIGUSR1)
3. Supervisor task protects gateway PID from taskkill — must use Stop-ScheduledTask/Start-ScheduledTask
4. ESM/CommonJS mismatch: ledger-emitter.js needed ESM conversion for dynamic import() to work

### L30 (new learning)
"Grafted code in dist/ does not load until the gateway process restarts. The supervisor task protects the gateway from taskkill /IM (safety feature, but blocks code reload). Use Stop-ScheduledTask/Start-ScheduledTask for supervisor-aware restart. Third instance of this pattern: SIGUSR1 stale code, Ledger module caching, emitter injection."

### Emitter debug status (end of session)
- Emitter module: BUILT, loads cleanly via require() with .cjs extension, 4 exports verified
- Injection point: line 2011 of dist/index.js, after worker.enqueue(captured), inside `if (captured.length > 0)` block
- Root cause of .js→.cjs: package.json has "type": "module", Node treats .js as ESM, module.exports invisible
- Gateway: restarted via Stop-ScheduledTask/Start-ScheduledTask multiple times, new PIDs confirmed
- Test result: agent_end fires (33 entries May 11), captures continue, BUT emitter code path never reached (zero log entries from emitter or the try/catch)
- DIAGNOSTIC ADDED: `console.error("EMITTER_INJECTION_REACHED")` before the try/catch. File saved, NOT yet restarted.
- OPEN QUESTION: Is line 2011 on the actual agent_end execution path, or is there a parallel/shadowed function definition that jiti loaded from a cached copy?
- The 5-min periodic trigger may be cycling gateway instances (PID changed from 518248→515772→525512) — possible that some restarts loaded old cached code

### Resolution (May 13)
- Root cause of "wrong file": gateway loads `index.ts` via jiti, NOT `dist/index.js`. `package.json` → `clawdbot.extensions: ["./index.ts"]`
- Jiti caches compiled output in `%TEMP%\jiti\`. Must delete cache after source edits.
- Injected into `index.ts` line 2315 → EMITTER IS FIRING (41 confirmed log entries May 12-13)
- Events ARE landing in Ledger (500+ total, 45 since May 12)
- event_subtype and memory_chunk_id columns not persisting (schema ALTER TABLE was on dist/db.js, not actual running DB)

### Next (after diagnostic)
- Resolve emitter firing → event 51 in Ledger
- "No pointer, no done" runtime enforcement (Gate 5)
- Plato model config + patch #4 (background)

### Aaron directives
- Schema v1.2 with event_subtype: APPROVED
- northstar_goals.json with 5 goals: APPROVED  
- Conservative event derivation (default memory_capture, don't auto-detect): CONFIRMED
- Failure handling (retry queue, never block agent_end): CONFIRMED
- Cross-machine implication: Ledger is now cross-machine truth source, not just governance. Schema fields are coordination medium. Treat with that weight.
