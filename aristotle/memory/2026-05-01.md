# 2026-05-01

## STRATEGIC REDIRECT — Aaron binding directive (supersedes all prior)

### Core thesis
Building the first multi-agent system that compounds knowledge across sessions, agents, and projects without model retraining. Measure: does session 10 require less effort than session 1?

### Key decisions
1. **lossless-claw custom data pipeline: CANCELLED.** MemOS ships this as a plugin. Building custom = negative ROI.
2. **openclaw-fork graft code: COLD STORAGE.** Preserved, not prioritized, don't build pipelines for it.
3. **MemOS Local OpenClaw Plugin: PRIMARY PATH.** Dissection first, then install.
4. **MemPalace data: IRREPLACEABLE.** 267K drawers must be preserved. MemOS must absorb or coexist.
5. **Full delegation authority** for technical execution. Escalate only strategic/scope decisions.

### Phase 1: Dissection (24h)
Daedalus reads MemOS Local OpenClaw Plugin. Six questions:
- Q1 (LOAD-BEARING): Works with Clawdbot 2026.1.24-3?
- Q2 (LOAD-BEARING): before_agent_start/agent_end hooks fire for sub-agents?
- Q3: MemOS-Hermes state sharing vs duplication?
- Q4: Standard mechanism inventory
- Q5: MemPalace absorption path (HIGH PRIORITY)
- Q6: Windows build verification

### Phase 2: Install + Gate 1
Conditional on Q1/Q2. Three paths:
- Q1+Q2 YES → Install on Alienware, run Gate 1 with L26 standard
- Q1 YES, Q2 NO → Orchestrator preamble workaround
- Q1 NO → Assess patchable vs architectural, escalate if architectural

### Phase 3: NorthStar Bridge (received in full)
- MemOS memory writes emit events → NorthStar Ledger records them
- Reactivates Ledger (first events since March)
- Bootcapsule continues reading from NorthStar (single source of truth for governance)
- "No pointer, no done" gets runtime enforcement via Ledger adapter (Gate 5)
- Schema discipline before volume: get format right with 5 real events before scaling
- Estimated 2-3 days custom work — the ONLY custom work in the plan

### Pre-install checks (Aaron, 00:46)
1. Patch upstreamability: GENERIC — file PR with MemTensor after validation
2. Runtime network verification: netstat before/after, 1h monitoring
3. MemPalace coexistence: domain-split policy (MemOS = sessions, MemPalace = domain knowledge)

### MemOS Install dispatched
Daedalus applying patches + installing on Alienware. Steps: patch → extension dir → config → network verify → plugin load verify.

### L26 is the validation standard for all gates
Structural connection ≠ integration. Data must flow. Tools with empty stores = plumbing without water.
