# 2026-06-03 — Ekhart Debug Marathon + State

## Fleet Status
- **AlienWare-Hermes (Neo)**: ✅ Running, stable, @Nietzsche247_bot
- **Empiricus-Hermes (Ekhart)**: ⚠️ Running (0 restarts) but Telegram NOT yet verified responsive
- **Plato**: ✅ up
- **Aristotle**: ✅ up
- **Comms Hub**: ✅ Port 3001

## Ekhart Root Cause (FOUND after ~3 hours debugging)
**Problem:** Hermes exits cleanly when no TTY detected ("Warning: Input is not a terminal (fd=0)")
- s6 supervisor kept restarting it → looked like crash loop
- 200+ restarts accumulated over the afternoon
- Gateway log was always 0 bytes (Telegram never started)
- state.db corruption from repeated restarts was a secondary issue (cleared it)

**Fix Applied:** `docker run -d -t` (adds pseudo-TTY with `-t` flag)
- Container: `nousresearch/hermes-agent:latest` on Empiricus
- Current restart count: 0
- Volumes, .env all intact
- Gateway connection to Telegram: NOT YET VERIFIED (Aaron couldn't test before context reset)

**Red herrings we chased:**
1. Nous auth (WARNING only, not crash cause)
2. Tirith binary missing (was a contributing slowness, not crash)
3. state.db corruption (secondary - cleaned)
4. hermes-tooled image entrypoint (no CMD, different issue)
5. 41.7GB docker commit (aborted, not needed)

## Exact Docker Run Command (WORKING on Empiricus):
```powershell
docker run -d -t `
  --name hermes `
  --restart unless-stopped `
  -p 9119:9119 -p 8642:8642 `
  -v "C:\Users\aaron\.hermes:/opt/data" `
  --env-file "C:\Users\aaron\.hermes\.env" `
  --add-host "host.docker.internal:host-gateway" `
  nousresearch/hermes-agent:latest
```
KEY: `-t` flag is REQUIRED. Without it, Hermes detects no TTY and exits.

## Empiricus .env (current state):
```
GATEWAY_ALLOW_ALL_USERS=true
HERMES_YOLO=true
OLLAMA_BASE_URL=http://host.docker.internal:11434
TELEGRAM_BOT_TOKEN=8779363929:AAE5G1VnWks_wVjGIpxZFrrHgHYm_8XZjlU
TELEGRAM_BOT_USERNAME=Ekhart247_bot
HOST_USER=aaron
HOST_NAME=NIETZSCHE-I9
TERMINAL_ENV=local
HERMES_SKIP_SETUP=true
OPENAI_API_KEY=ollama
```

## Pending After Reset:
1. **Verify Ekhart responds** — Aaron tries /start in @Ekhart247_bot
2. **If no response:** Check gateway log `C:\Users\aaron\.hermes\logs\gateways\default\current`
3. **Ollama model**: Empiricus has llama3.2 (2GB) + qwen2.5:14b (pulling, 9GB). Config set to llama3.2.
4. **After Ekhart confirmed working:** /sethome in bot
5. **SSH keys**: Both Hermes instances can SSH to each other ✅ (wired today)
6. **NorthStar onboarding**: Daedalus+Thales done. Ledger rule at TOP of SOUL.md.
7. **Twilio**: A2P campaign pending (~2-3 weeks). No action needed.
8. **Structured graph context_capsule**: Low priority improvement for Ledger. Worth doing later.
9. **hermes-authed:latest**: 41.7GB image sitting on AlienWare. Can delete to free space.

## Why Ekhart warnings on Empiricus:
The Nous auth warnings (`Auxiliary Nous client unavailable`) fire every ~60s.
These are WARNINGS only — not crash-causing. Hermes works fine without Nous auth.
They'll keep appearing in logs. Ignore them. They don't affect Telegram functionality.
