# ============================================================
# Comms Hub — Environment Configuration
# ============================================================
# Gateway URLs default to correct values in server.js code.
# Only add URL overrides here if network topology changes.
# Tokens authenticate push delivery to each agent's gateway.
# ============================================================

# API key — protects env registry endpoints
ENV_API_KEY=[PLACEHOLDER_WJDB_SECRET]

# Gateway auth tokens for push delivery (per-agent)
GATEWAY_TOKEN_ARISTOTLE=2461f6603a12be7834554741068559144df6921f7618d124
GATEWAY_TOKEN_PLATO=[PLACEHOLDER_AD87_SECRET]
GATEWAY_TOKEN_EMPIRICUS=[PLACEHOLDER_SECRET_911A]

# Subagents (same machine — no token needed unless secured later)
GATEWAY_TOKEN_DAEDALUS=
GATEWAY_TOKEN_THALES=
GATEWAY_TOKEN_STEELMAN=
GATEWAY_TOKEN_RESEARCHER=

# Stigmergy Browser Server (Hetzner)
BROWSER_SERVER_URL=http://5.78.186.135:3000
BROWSER_API_TOKEN=[PLACEHOLDER_SECRET_7E2F]
