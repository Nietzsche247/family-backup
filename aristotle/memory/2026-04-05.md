# 2026-04-05

## Self-Healing Check — 08:00 MST

### Service Status
| Service | Status |
|---------|--------|
| comms-hub | ✅ Online (2D uptime) |
| ledger | ✅ Online (2D uptime) |
| ledger-staging | ✅ Online (2D uptime) |
| Ollama (11434) | ✅ Online |
| cloudflared | ⚠️ Service was down — restarted manually |

### Cloudflared Issue
- **Symptom:** Windows service fails with error 1067 (process terminated unexpectedly)
- **Root cause:** Service runs as SYSTEM user, reads config from `C:\Windows\System32\config\systemprofile\.cloudflared\config.yml`. Even after copying config + creds to SYSTEM profile, fails because SYSTEM can't load Windows root certificate pool (cloudflared explicitly logs this limitation).
- **Fix applied:** Started cloudflared manually as `aaron` user via `cmd /c C:\temp\run-cf.bat`. Tunnel connected successfully with 4 QUIC connections to Cloudflare LAX PoPs.
- **Permanent fix needed:** Either:
  1. Configure cloudflared service to run as `aaron` user instead of SYSTEM, OR
  2. Use `--origin-ca-pool <PATH>` flag in the service config, OR
  3. Add cloudflared to pm2 under aaron's user context (most reliable, matches comms-hub pattern)
- **Permanent fix applied:** Added cloudflared to pm2 (id=3) with autorestart. Config: `C:\temp\cf-ecosystem.config.js`. pm2 state saved. Disabled the broken Windows service (`sc.exe config cloudflared start= disabled`). Now managed identically to comms-hub and ledger.

### Daedalus Backup
- Daily backup completed at 04:30 MST. Pushed to github.com/Nietzsche247/family-backup. Secrets scrubbed. No errors.
