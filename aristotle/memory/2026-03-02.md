# 2026-03-02 — v3 Declared Operational

## Milestone
Aaron accepted all three truth gates at 13:33 MST. System declared operational.

## What was built today
- **P0** (commit `6643600`): event_id NOT NULL UNIQUE, server-side ULID authority, NULL backfill/quarantine migration
- **P1** (commits `51f0469`, `c59c053`, `b4a7105`): Embedded reconciler with single-runner lease, jitter+backoff, per-cycle cap, degraded mode, atomic server-stamped replication_verified_at_utc
- **P2** (commit `6643600`): PATCH auth locked — 403/400/404 correct

## Key decisions
- event_id: server-side ULID only. Client values ignored. event_slug is cosmetic only.
- replication_verified_at_utc: server-stamped atomically with replicated status. Client cannot override.
- Reconciler: embedded in server.js with lease-based single-runner. Fine for now; may split to own process at scale.
- shared_root creation: scripted, idempotent (`scripts/setup-shared-root.js`)
- PM2 pipe issue: `//./pipe/rpc.sock` EPERM for non-elevated. Workaround: elevated PM2 with PM2_HOME=C:\Users\aaron\.pm2_fresh. Needs reboot to fully fix.

## Aaron's strategic framing
> "A system that admits when it's degraded, heals itself, and leaves an audit trail. That condition is now satisfied."
> "You have crossed from 'Experiment with clever recovery scripts' to 'Self-healing audited infrastructure with enforced invariants.'"

## Watch items (not blockers)
- Embedded reconciler in server.js — may need own process if load grows
- ULID generation must stay singular (Ledger only)

## Phase A — Signal/Compliance Enforcement (22:43 MST)
- Aaron accepted Phase A as Operational (Guarded → Live)
- Signal Catalog v1.1 implemented: authority_drift, authority_malfunction, contract_claim_without_proof, invalid_pointer_reference, ledger_unreachable, override_abuse
- All S3 blocking is binary (confidence=1.0), no probabilistic enforcement
- Steel Man reviewed and vetoed: missing_pointers split, authority_drift override removed in prod, "done" removed from contract phrases
- Idempotency fix: deterministic dedupe_key = sha256(conversation_id + message_id + signal_type + subject_scope), UNIQUE enforced at DB
- Contract claim classifier: requires contract token + completion verb (no generic language)
- Commits: 5105d35, ed28dc7

## Next (Aaron directive)
- 2 weeks observability + false-positive monitoring before adding any new S3
- Follow-ups (not blockers): metrics/rate visibility, ledger_unreachable drill
- Phase B: InfraNodus radar when ready
